# stim-cxx

Low-level vendored C++ bridge crate for `stim`.

This crate packages the minimal Stim C++ source subset required to build the Rust bindings on crates.io and docs.rs without depending on an external git submodule checkout.
